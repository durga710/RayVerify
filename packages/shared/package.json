{
  "name": "@rayverify/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript contracts (enums, DTO shapes, risk helpers) for RayVerify backend & frontend.",
  "license": "UNLICENSED",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
